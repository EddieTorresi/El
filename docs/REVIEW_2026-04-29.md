# El — Review & Release Notes (v3 → v5)

Originally a code-review doc; updated to track what actually shipped.

---

## Status of original review items

| Item | Status | Notes |
|---|---|---|
| Restore truncated `index.html` | ✅ shipped (v3) | `git checkout` + pre-commit hook + GitHub Action enforce it |
| Pre-commit hook to prevent truncation | ✅ shipped (v3) | `scripts/check-integrity.sh` + `scripts/pre-commit-hook.sh` |
| GitHub Action mirror of integrity check | ✅ shipped (v3) | `.github/workflows/integrity.yml` |
| SW cache name tied to BUILD | ✅ shipped (v3) | `sw.js` reads `BUILD` constant |
| Visible build tag in Settings | ✅ shipped (v3) | `window.EL_BUILD` rendered at the bottom of Settings |
| 30-day backup reminder | ✅ shipped (v3) | `lastExportAt` stamp + Home banner |
| `escapeHtml` AI confirmation cards | ✅ shipped (v3) | Closed the only realistic XSS path |
| Manifest icon trim | ✅ shipped (v3) | Dropped SVG entries, PNG-only with `purpose:"any"` |
| `netWorthHistory` cap raised | ✅ shipped (v3) | 90 → 1100 entries (~3 years) |
| In-file dedup (`catOptions` helper, `DEFAULT_CATEGORIES`) | ✅ shipped (v3) | |
| Console gating behind `El.debug` | ✅ shipped (v3) | Expected-path logs hidden by default |
| Financial Health score rewrite | ✅ shipped (v3) | 5-component weighted with breakdown modal |
| El AI tab gating on API key | ✅ shipped (v3) | Bottom nav hidden until configured + capture banner |
| Job Loss Runway auto-fill | ✅ shipped (v3) | Pulls avg of last 3 months of expense txns |
| Capture mic visibility fix | ✅ shipped (v3) | Always visible, toast explains iOS PWA limitation |
| Latest models in pickers | ✅ shipped (v4) | Claude Opus 4.6 / Sonnet 4.6 / Haiku 4.5; OpenAI GPT-4.1 / 4o / mini variants |
| Auto model routing | ✅ shipped (v4) | `pickClaudeModel` / `pickOpenAiModel` route by length + intent |
| Anthropic prompt caching + data caps + instruction trim | ✅ shipped (v4) | Static block cached, dynamic block capped |
| `set_budget` AI action | ✅ shipped (v4) | Chat can now update or create budget allocations |
| OK to Buy? feature | ✅ shipped (v4) | What If card with instant local verdict + optional AI deep-dive; chat parity |
| Tappable per-category budget editing | ✅ shipped (v5) | Tap any Budget category to set/change/delete |
| "Reset all allocations" button | ✅ shipped (v5) | Clears the legacy demo $2,250 carryover |

## Still open

- **Module split.** Single-file `index.html` is intentional for the testing phase. Defer until behavior stabilizes.
- **Server-side AI proxy.** Required before letting anyone else use the app — currently each user must bring their own key and that key sits in plaintext localStorage.
- **Encrypted at-rest storage for keys.** `aiApiKey` and `gcalToken` should be wrapped in WebCrypto if the app ever leaves solo testing.
- **Maskable-safe icon.** PNGs use `purpose:"any"`; a dedicated maskable PNG with 80% safe-zone would render cleaner on Android.
- **Inline confirmation cards.** Toasts cover the basics, but the testing log notes inline cards may be richer UX.
- **Finance Clarity Engine — phase 2.** OK-to-Buy was phase 1. Phase 2 ideas: scenario comparison, retirement readiness, life-goal projections.

See `CONTEXT.md` for the canonical "what shipped" table and architectural rules. See `PERSONAL_TESTING_LOG.md` for the running personal-testing journal with v3 / v4 / v5 entries.
